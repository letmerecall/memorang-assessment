# Frontend

Next.js + CopilotKit UI for the Memorang AI Learning Agent.

Setup, architecture, environment variables, and run instructions live in the **[root README](../README.md)**.

```bash
# From repo root — see root README for full setup
cd frontend && npm install && npm run dev
```
